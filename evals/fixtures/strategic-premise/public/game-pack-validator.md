# Game-pack validator

The repository needs deterministic validation that a Godot game pack declares a valid manifest, stays inside its namespace, uses an allowed SDK version, and reproduces the same artifact hash. Decide whether to build a repository tool or integrate an existing capability, then propose the change.
