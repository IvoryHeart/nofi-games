# Cross-platform distribution

The product needs one app containing a changing catalog of games on web, desktop, Android, and iOS. Propose the architecture, including whether the repository should build its own game engine, packaging system, application shell, and deployment coordinator.
